type Mutation {
  addTransaction(date: Date!, details: String, amount: Int!): Transaction!
  updateTransaction(id: ID!, date: Date!, details: String, amount: Int!): Transaction!
  deleteTransaction(id: ID!): Transaction!
}